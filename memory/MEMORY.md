# LedgerProof Memory

## Durable decisions

- LedgerProof is an independent sibling project, not a CyberGuard subdirectory.
- The demo must use CyberGuard at runtime through its executor container image.
- Agents investigate and propose; a human approves; only the executor changes the payment twin.
- Public claims must distinguish deterministic financial fixtures from real governance enforcement.
